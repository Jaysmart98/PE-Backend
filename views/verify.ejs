<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification Status</title>
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    },
                    colors: {
                        'primary-green': '#10b981', // Emerald 500
                        'primary-red': '#ef4444',   // Red 500
                    }
                }
            }
        }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /* Ensures full screen height */
        body {
            min-height: 100vh;
        }
    </style>
</head>
<body class="bg-gray-50 flex items-center justify-center font-sans p-4">

    <!-- Main Container -->
    <div id="verification-container" class="w-full max-w-lg mx-auto bg-white rounded-xl shadow-2xl p-8 md:p-12 text-center transform transition duration-500 scale-100">
        
        <!-- Loading State -->
        <div id="loading" class="flex flex-col items-center justify-center p-6 space-y-4">
            <svg class="animate-spin h-8 w-8 text-gray-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            <h2 class="text-xl font-semibold text-gray-700">Processing Verification...</h2>
        </div>

        <!-- Success State (Hidden by default) -->
        <div id="success-state" class="hidden">
            <svg class="w-20 h-20 mx-auto mb-6 text-primary-green" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <h1 class="text-3xl font-extrabold text-primary-green mb-4">Success!</h1>
            <p class="text-gray-600 mb-8 text-lg">Your email address has been successfully verified. You can now access all features of **[Your App Name]**.</p>
            <a href="/" class="inline-block px-8 py-3 bg-primary-green hover:bg-emerald-600 text-white font-bold rounded-full transition duration-150 shadow-lg shadow-primary-green/50">
                Go to Dashboard
            </a>
        </div>

        <!-- Failure State (Hidden by default) -->
        <div id="failure-state" class="hidden">
            <svg class="w-20 h-20 mx-auto mb-6 text-primary-red" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <h1 class="text-3xl font-extrabold text-primary-red mb-4">Verification Failed</h1>
            <p class="text-gray-600 mb-8 text-lg">The verification link is invalid, expired, or has already been used. Please try logging in or requesting a new verification email.</p>
            <a href="/resend-verification" class="inline-block px-8 py-3 bg-gray-600 hover:bg-gray-700 text-white font-bold rounded-full transition duration-150 shadow-lg">
                Resend Email
            </a>
            <a href="/" class="block mt-4 text-sm text-gray-500 hover:text-gray-700 transition duration-150">
                Go to Home
            </a>
        </div>

    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const loading = document.getElementById('loading');
            const successState = document.getElementById('success-state');
            const failureState = document.getElementById('failure-state');

            // --- MODIFIED LOGIC START ---
            // Removed query parameter check to always show success
            
            // Simulate server check delay
            setTimeout(() => {
                loading.classList.add('hidden');

                // Always show success state
                successState.classList.remove('hidden');
                document.title = 'Verification Complete!';
                
            }, 1500); // 1.5 second loading delay
            // --- MODIFIED LOGIC END ---
        });
    </script>
</body>
</html>
